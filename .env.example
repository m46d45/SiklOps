# Neon/Postgres — set this on the siklops Vercel project (not parade-tim-kerja).
# Fastest: Vercel → siklops → Storage → Create Database → Neon.
# Or paste a URI from console.neon.tech. Vercel Storage also injects POSTGRES_URL.
DATABASE_URL=
# POSTGRES_URL=

# Login is off unless you set this to true at build time.
# VITE_AUTH_ENABLED=true
